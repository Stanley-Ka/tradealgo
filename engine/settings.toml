[project]
name = "trading-engine"
mode = "dev"            # dev | backtest | paper | live
timezone = "US/Eastern"

[data]
# storage_root is where Parquet and caches live; keep this out of git
storage_root = "C:\EngineData"
universe = "sp500"       # or custom list
bar_size = "1D"          # 1D | 240min | 60min

[brokers]
default = "ibkr"         # ibkr | tradier

[risk]
max_name_weight = 0.10
max_gross = 1.00
max_net = 0.50
sector_max = 0.30

[sizing]
alpha = 1.0
w_max = 0.10
